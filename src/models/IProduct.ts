export interface IProduct {
  id: number;
  imageUrl: string;
  name: string
  price: number;
  category: string;
  description: string;
}
export interface IPages {
    name: string
    path: string
}