import React, { FC } from "react";
import { Box, Container } from "@mui/material";

const Cartpage: FC = () => {
  return (
    <Container fixed>
      <Box sx={{ bgcolor: "#cfe8fc", height: "100vh" }} />
    </Container>
  );
};

export { Cartpage };
