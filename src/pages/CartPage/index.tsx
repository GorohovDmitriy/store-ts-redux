import React, { FC } from "react";
import { Box, Container } from "@mui/material";

const CartPage: FC = () => {
  return (
    <Container fixed>
      <Box />
    </Container>
  );
};

export { CartPage };
